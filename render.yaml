services:
  - type: static
    name: baraka-devfolio
    buildCommand: |
      npm install
      chmod +x ./node_modules/.bin/vite || true
      npx vite build
    publishPath: dist
    envVars:
      - key: NODE_VERSION
        value: 22.16.0

